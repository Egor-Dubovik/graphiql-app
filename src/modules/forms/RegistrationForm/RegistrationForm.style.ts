import { makeStyles } from '@mui/styles';

const useRegistrationFormStyles = makeStyles({
  'register-form': {},
  'register-form__input': {
    width: '100%',
  },
});

export default useRegistrationFormStyles;
