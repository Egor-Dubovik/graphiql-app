import { makeStyles } from '@mui/styles';

const useResetFormStyles = makeStyles({
  'reset-form': {},
  'reset-form__input': {
    width: '100%',
  },
  'reset-form__button': {
    width: '100%',
    marginBottom: '15px',
  },
});

export default useResetFormStyles;
