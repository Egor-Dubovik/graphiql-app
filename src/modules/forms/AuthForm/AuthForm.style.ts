import { makeStyles } from '@mui/styles';

const useAuthFormStyles = makeStyles({
  auth__form: {
    maxWidth: '600px',
    padding: '20px 15px',
  },
});

export default useAuthFormStyles;
