import { makeStyles } from '@mui/styles';

const useAuthPageStyles = makeStyles({
  container: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default useAuthPageStyles;
